# Roadmap

Planned work for the OCS Inventory Docker stack.

- [ ] Support MariaDB (in addition to PostgreSQL).
- [ ] Provide example override files for common deployments (external reverse proxy / TLS offload, split-origin front and API).
